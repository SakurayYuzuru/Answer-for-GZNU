# Database
## 绪论
### 数据库系统概述
1. 定义： 数据库是*长期储存*在计算机内*有组织*，*可共享*的*大量数据的集合*
2. 数据库的基本特征：
   1. 数据按照一定的数据模型组织，描述和储存
   2. 较小的*冗余度*
   3. 较高的*数据独立性*
   4. 可扩展性
3. 什么是数据库管理系统：
   1. 位于*用户*与*操作系统*之间的一层*数据管理软件*
   2. 计算机的基础软件，是一个大型复杂的软件系统 
4. 数据库管理系统的主要功能：
   1. *数据定义功能*：
      1. 提供数据定义语言(*DDL*)
      2. 定义数据库中的*数据对象的组成与结构*
   2. *数据组织，存储和管理功能*
      1. 分类组织，存储和管理各种数据
      2. 确定组织数据的*文件结构和存取方式*
      3. 实现数据之间的*联系*
      4. 提供多种*存取方法*提高*存取效率*
   3. *数据操纵功能*
      1. 提供数据操纵语言(*DML*)
      2. 实现对数据库的*基本操作*(*查询，插入，删除和修改*)
   4. 数据库的*事务管理和运行管理功能*
      1. 数据库在建立，运行和维护时由数据库管理系统*统一管理和控制*
      2. 保证数据的*安全性，完整性*
      3. 多用户对数据的并发使用及发生故障后的*系统恢复*
   5. 数据库的*建立和维护功能*
      1. 数据库*初始数据的输入和转换*
      2. 数据库*转储和恢复功能*
      3. 数据库的*重组织，性能监视和数据分析*等
   6. 其他功能
      1. 数据库管理系统与网络中其他软件系统的*通信*
      2. 数据库管理系统之间与文件系统到的*数据转换*
      3. 异构数据库之间的*互访和互操作*
5. 数据库系统的构成
   1. 数据库
   2. 数据库管理系统(及外围的应用开发工具)
   3. 应用程序
   4. 数据库管理员
6. 数据管理技术的发展过程
   1. 人工管理阶段
   2. 文件系统阶段
   3. 数据库系统阶段
7. *整体数据的结构化*是数据库的主要特征之一
   1. 数据记录可以*变长*
   2. 数据的最小存取单位是*数据项*
8. 数据用*数据模型*描述，无需应用程序定义
9. 数据的独立性：
   1.  *物理独立性*：
       1.  *用户的应用程序与数据库中数据的物理存储是相互独立的*
       2.  *当数据的物理存储改变了，应用程序不用改变*
   2.  *逻辑独立性*：
       1.  *指用户的应用程序与数据库的逻辑结构是相互独立的*
       2.  数据的逻辑结构改变了，应用程序不用改变
10. 数据库管理系统提供的数据控制功能：
    1.  数据的*安全性*保护
    2.  数据的*完整性*检查
    3.  数据的*并发控制*
    4.  数据库的*恢复*

### 数据模型
1. 数据模型是*对现实世界数据特征的抽象，是现实世界的模拟*
2. 数据模型是用来*描述数据，组织数据和对数据进行操作的*
3. 数据模型应满足三方面要求：
   1. 能比较*真实*地模拟现实世界
   2. *容易为人所理解*
   3. 便于在计算机上*实现*
4. 数据模型是数据库系统的*核心和基础*
5. 数据建模过程(*两步抽象*)：
   1. *现实世界中的客观对象抽象为概念模型*
   2. *把概念模型转换为某一数据库管理系统支持的数据模型*
6. 概念模型的用途：
   1. 现实世界到信息世界的*第一层抽象*
   2. *数据库设计人员和用户之间进行交流的语言*
7. 概念模型的一种表示方法：*实体-联系模型*
   1. *实体*
   2. *属性*
   3. *码*
   4. *实体型*
   5. *实体集*
   6. *联系*
8. 数据模型三要素：
   1. *数据结构*
      1. 描述*数据库的组成对象以及对象之间的联系*
      2. 数据结构是*系统静态特性*的描述
   2. *数据操纵*
      1. 对*数据库中各种对象（型）的实例（值）允许执行的
操作的集合，包括操作及有关的操作规则*
   3. *完整性约束*
      1. 一组*完整性规则*
      2. 完整性规则：*给定的数据模型中数据及其联系所具有的制约和依存规则*
      3. 物理模型：*数据模型以一定的组织方式存储在数据库管理系统中，是数据模型在数据库管理系统内部的物理存储结构*
      4. 描述数据在数据库管理系统内部的数据组织和存取方法的实现，包括*存储结构和索引结构*的物理实现
9. 层次模型用*树形结构*来表示各类实体以及实体间的联系
10. 满足下面两个条件的基本层次联系的集合为层次模型：
    1. 有且只有*一个结点没有双亲结点*，这个结点称为*根结点*
    2.  根以外的其他结点*有且只有一个双亲结点*
11. 层次模型特点：只能直接处理*一对多*的实体联系
12. 层次模型优点：
    1.  *查询效率高*
    2.  层次数据模型提供了良好的*完整性约束支持*
13. 网络模型的优点：*存取效率高*
14. 关系模型的数据结构
    1.  关系：*一个关系对应通常说的一张表*
    2.  元组：*表中的一行即为一个元组*
    3.  属性：*表中的一列即为一个属性，给每一个属性起一个名称即为属性名*
    4.  码：*表中的某一个属性或一组属性，它的值可以唯一确定一个元组*
    5.  域：*是一组具有相同数据类型的值的集合，属性的取值范围来自某个域*
    6.  分量：*元组中的一个属性值*
    7.  关系模式：对关系的描述
15. 关系模式最基本的规范条件：关系的每一个分量必须是一个*不可分的数据项, 不允许表中还有表*
16. ***关系模型的优缺点***：
    1.  优点：
        1.  建立在严格的数学概念的基础上
        2.  概念单一
        3.  关系模型的存取路径对用户是隐蔽的
    2.  缺点：
        1.  存取路径对用户隐蔽，查询效率往往不如层次模型和网状模型
        2.  为提高性能，必须对用户的查询请求进行优化，增加了开发数据库管理系统的难度

### 数据库系统的组成
1. 
    ```mermaid
    graph TD
        subgraph Client to App
            A[客户端] <--> B[应用系统]
        end

        subgraph Database Structure
            direction LR
            C[外模式] <--> D[数据库管理系统]
            E[模式] <--> D
            F[内模式] <--> D
            G[数据库] <--> D
        end

        subgraph Storage
            direction LR
            G --> H[操作系统]
        end

        B <--> D
        D <--> H
    ```

## 关系模型
### 关系
1. 现实世界的*实体以及实体之间的各种联系*均用关系来表示
2. ***基本关系的性质***:
   1. 列是同质的
   2. 不同的列可出自同一个域:
      1. 其中的每一列称为一个属性
      2. 不同的属性要给予不同的属性名
   3. 列的顺序无所谓,列的次序可以任意交换
   4. 任意两个元组的码不能相同
   5. 行的顺序无所谓，行的次序可以任意交换
   6. 分量必须取原子值，即每一个分量都必须是不可分的数据项
3. 关系模式是型，关系是值
4. $R(U, D, DOM, F)$
   1. `DOM`是属性向域的映像集合
   2. `F`是属性间数据的以来关系集合
5. 候选码：***关系模式中的某一个属性或一组属性的值能唯一地标识一个元组，而它的真子集不能唯一地表示一个元组，则称该属性或属性组为候选码***
6. 在一个关系数据库中，某一时刻所有关系模式对应的关系的集合构成一个关系数据库

### 关系操作
1. ***基本的关系操作***: 选择，投影，并，差，笛卡尔积
2. 特点:
   1. 操作的对象和结构都是集合，一次一个集合
   2. 输入和输出均是关系，包括关系操作的中间结果也是关系

### 关系的完整性
1. ***实体完整性规则***:
   1. 若属性A是基本关系R的主属性，则A不能取空值
   2. 空值就是“不知道”或“不存在”或“无意义”的值
   3. 主码中的属性不能取空值
2. ***参考完整性***:
   1. 设F是基本关系R的一个或一组属性，但不是关系R的码，Ks是基本关系S的主码。如果F与Ks相对应，则称F是R的外码
   2. 基本关系R称为参照关系
   3. 基本关系S称为被参照关系或目标关系
3. 参考完整性约束: 若属性F是基本关系R的外码，它与基本关系S的主码Ks相对应，则对于R中每个元组在F上的值必须:
   1. 或者取空值
   2. 或者等于S中某个元组中的值

### 关系代数
1. 并: $R \cup S = \{t | t \in R \lor t \in S\}$
2. 差: $R - S = \{t | t \in R \land t \notin S\}$
3. 交: $R \cap S = \{t | t \in R \land t \in S\}$
   > $R \cap S = R - (R - S)$
4. 笛卡尔积: $R \times S = \{\overgroup{t_{r}t_{s}} | t_{r} \in R \land t_{s} \in S\}$

## 关系数据库标准语言SQL
### SQL概述
1. `SQL`是结构化查询语言，是关系数据库的标准语言，包括*数据查询*，*数据库模式创建*，*数据库数据的增删改*，*数据库安全性和完整性定义与控制*等
2. `SQL`集数据定义语言(`DDL`)，数据操纵语言(`DML`)和数据控制语言(`DCL`)的功能于一体
   1. 创建和删除数据库模式(`DDL`)
   2. 创建基本表，创建视图
   3. 使用数据库，如查询和增删改数据，事物处理等(`DML`)
   4. 数据库控制，如安全性控制，完整性控制和并发控制等(`DCL`)
   5. 数据库维护和重构，如修改和删除基本表，数据库备份与恢复等
3. 基本表：
   1. 本身独立存在的表
   2. 关系数据库管理系统中一个关系就对应一个基本表
   3. 一个或多个基本表对应一个存储文件
   4. 一个表可以带若干索引
4. 逻辑结构和物理结构组成了关系数据库的内模式

### 数据定义
1. 定义模式
   `CREATE SCHEMA [<schema-name>] AUTHORIZATION <user-name>`
   定义了一个命名空间(namespace)
2. 删除模式
   `DROP SCHEMA <schema-name> <CASCADE | RESTRICT>`
   1. `CASCADE`在删除模式的同时把该模式中所有的数据库对象全部删除
   2. `RESTRICT`如何在该模式中已经定义了数据库对象，则拒绝该删除语句的执行
3. 定义基本表
   ```sql
   CREATE TABLE <table-name>(
      <列名> <数据类型> [列级完整性约束],
      <列名> <数据类型> [列级完整性约束],
      ...
   )
   ```
4. 数据类型: `DATE`, `TIME`
5. 删除基本表
   `DROP TABLE <table-name> [RESTRICT | CASCADE]`
   表上建立的索引、视图、触发器等一般也将被删除
6. 建立索引
   ```sql
   CREATE [UNIQUE] [CLUSTER] INDEX <index-name>
      ON <表名>(<列名> [<次序>] [, <列名> [<次序>]], ...)
   ```
   `UNIQUE`此索引的每一个索引值只对应唯一的数据记录
   `CLUSTER`表示要建立的索引是聚簇索引
7. 删除索引
   `DROP INDEX <index-name>`
8. `%`任意长度的字符串
9. `_`任意单个字符
10. 空值用`IS NULL`或`IS NOT NULL`
11. 视图是从一个或几个基本表导出的表。它与基本表不同，是一个虚表
12. `WITH CHECK OPTION`表示对视图进行`UPDATE`，`INSERT`和`DELETE`操作时，要保证更新、插入或删除的行满足视图定义中的谓词条件
13. `CREATE VIEW`只是把视图的定义存入数据字典，并不执行其中的`SELECT`语句
14. 若一个视图是从单个基本表导出的，并且只是去掉了基本表的某些行和某些列，但保留了主码，则称这类视图为行列子集视图
15. 删除视图
   `DROP VIEW <view-name>[CASCADE]`
   视图的定义从数据字典中删除
16. 视图消解：
   1.  有效性检查
   2.  转换成等见的对基本表的查询
   3.  执行修正的查询
17. 视图的作用：模式->外模式

## 数据库安全性
1. 计算机系统的安全性问题可分为: 
   1. 技术安全类
   2. 管理安全类
   3. 政策法律类
2. 数据库的安全性是指保护数据库以防止不合法使用所造成的数据泄漏、篡改或破坏
3. $C_{1}$级：实现对*用户和数据的分离*，*进行自主存取控制*
4. $B_{1}$级：对标记的*主体和客体实施强制存取控制*
5. 数据库管理系统的安全性控制模型：
   1. 事前控制：通过身份鉴别和入侵检测共同实现
   2. 事中控制：在SQL处理层提供多层访问控制
   3. 事后控制：配置审计规则，对用户访问行为和关系关键操作进行审计
   4. 存储保护：在数据存储层，对敏感数据、重要的存储过程定义加密存储
   5. 传输保护：提供了传输加密功能
6. 用户标识：由用户名和用户标识号组成
   > 用户标识号在系统整个生命周期内唯一
7. ***定义用户权限和合法权限检查机制一起组成了数据库管理系统的存取控制子系统***
8. 用户对某一数据对象的操作权力称为权限
9. 数据库管理系统必须提供适当的语言来定义用户权限，这些定义经过编译后存储在数据字典中，被称作安全规则或授权规则
10. 常用存取控制方法：
   1. 自主存取控制
   2. 强制存取控制
11. 自主存取控制方法通过`SQL`的`GRANT`和`REVOKE`实现
12. `WITH GRANT OPTION`可以再授权，不能传播，不允许循环授权
13. `GRANT ALL PRIVILIGES`全部权限
14. 把对表SC的查询权限授予所有用户
   ```sql
   GRANT SELECT
      ON TABLE SC
      TO PUBLIC
   ```
15. 收回所有用户对表SC的查询权限
   ```sql
   REVOKE SELECT
      ON TABLE SC
      FROM PUBLIC
   ```
16. 角色是权限的集合
17. ***主体是系统中的活动实体，客体是系统中的被动实体，受主体操纵***
18. ***敏感度标记***：
   1. 绝密
   2. 机密
   3. 可信
   4. 公开
   主体的敏感度标记为许可证级别，客体的敏感度标记为密级
19. ***强制存取控制规则***
   1. 仅当主体的许可证级别*大于或等于*客体的密级时，该主体才能读取相应的客体
   2. 仅当主体的许可证级别*小于或等于*客体的密级时，该主体才能写相应的客体
20. 实现MAC时要首先实现DAC
21. 通过视图机制，把要保密的数据对无权存取这些数据的用户隐藏起来，对数据提供一定程度的安全保护
22. 视图机制配合授权机制，间接地实现支持存取谓词的用户权限定义
23. 审计：把用户对数据库的所有操作自动记录放入审计日志
24. 根据一定的算法将原始数据(明文)经过一系列复杂计算，变换为不可直接识别的格式(密文)
25. 内核级加密方法：性能较好，安全性较高
26. 链路加密：
   1. 传输信息由报头和报文两部分组成
   2. 报文和报头均加密
27. 端到端加密：
   1. 只加密报文，不加密报头

## 数据库完整性
### 数据库完整性概述
1. 数据库的完整性:
   1. 数据的正确性
   2. 数据的相容性
2. 完整性检查和防范对象是不合语义的、不正确的数据
3. ***拒绝(NO ACTION)执行该操作或级联(CASCADE)执行其他操作，设置控制***

### ***参照完整性***
1. 在`CREATE TABLE`中用`FOREIGN KEY`定义那些列为外码，用`REFERENCE`指明这些外码参照哪些表的主码
2. ***违约处理***:
   1. 拒绝执行
   2. 级联操作
   3. 设置为控制
3. 属性上的约束:***检查列值是否满足一个条件表达式(CHECK短语)***
4. ***执行触发器***:
   1. 执行该表上的`BEFORE`触发器
   2. 激活触发器的`SQL`语句
   3. 执行该表上的`AFTER`触发器

## 关系数据理论(概念题)
1. 在$R(U, F)$中，如果$X \rarr Y$，并且对于$X$的任何一个真子集$X^{'}$都有$X^{'} \not \rarr Y$，则称$Y$对$X$完全函数以来，记作$X \overset{F}{\rarr} Y$
   若$X \rarr Y$，但$Y$不完全函数依赖于$X$，则称$Y$对$X$部分函数依赖，记作$X \overset{P}{\rarr} Y$
2. 在$R(U, F)$中，如果$X \rarr Y(Y \not \subseteq X), Y \not \rarr X, Y \rarr Z, Z \not \subseteq Y$，则称$Z$对$X$传递函数依赖，记作$X \overset{T}{\rarr} Z$
   如果$Y \rarr X$，则 $X \gets \rarr Y$,实际上是直接函数依赖不是传递函数依赖
3. 一个低一级范式的关系模式通过模式分解可以转换为若干个更高级别范式的关系模式的集合，这种过程叫做***规范化***
4. 2NF
5. 3NF

## 数据库设计
1. 数据库设计的目标是为用户和各种应用系统提供*信息基础设施*和高效的*运行环境*。其中高效的运行环境指数据库数据的*高存取效率*、数据库存储空间的*高利用率*和数据库系统运行维护的*高效率*等
2. ***两条准则***:
   1. 属性不能再具有需要描述的性质。属性必须是不可分的数据项，不能包含其他属性
   2. 属性不能与其他实体具有联系。`E-R图`中所表示的联系是实体之间的联系
3. 各子系统的`E-R图`之间的冲突:
   1. 属性冲突
   2. 命名冲突
   3. 结构冲突
4. 规范化到什么程度需要*权衡相应时间*和*潜在问题*两者的利弊关系

## 数据库编程
1. ***过程化`SQL`程序可以被命名和编译并保存在数据库中，称为存储过程或存储函数***

## 关系数据库存储管理
1. 优点: 
   1. 存储效率高，节省存储空间
   2. 存取效率高，访问速度快
2. 索引的优势:
   1. 一个表的索引块数量通常比数据块数量少的多
   2. 高效的方法在索引中快速查找
   3. 如果其索引文件足够小则可以让其长久的驻留在内存缓冲区中，减少了I/O操作
   索引会带来额外的开销，包括*存储空间的开销*，*建立索引的开销*，以及*维护索引的开销*

## 关系查询处理和查询优化
1. 基于启发式规则的优化:
   1. 选择操作的启发式规则:
      1. 对于小关系，使用**全表顺序扫描**，即使选择列上由索引
      2. 对于大关系:
         1. 如果“主码 = 值”，采用**主码索引扫描**
         2. 条件是”非主属性 = 值“，如果比例<10%，采用**索引扫描方法**，否则**全表顺序扫描**
         3. 用`AND`连接的，采用**组合索引扫描方法**;如果某些属性由单属性索引，采用**索引扫描方法**,否则**全表顺序扫描**
         4. 用`OR`连接的，一般使用**全表顺序扫描**
   2. 连接操作的启发式规则;
      1. 如果两个表都已经按照连接属性排序，则选用**排序-合并算法**
      2. 如果一个表中连接属性上由索引，则可以选用**索引连接算法**
      3. 如果上面两个规则都不适用，其中一个表较小，则可以选用**哈希连接算法**
      4. 最后可以选用**嵌套循环连接算法**，并选择其中较小的表，确切的将是占用的块数(B)较少的表，作为外表

## 作业题
1. 某工业园区需要开发一个信息系统，请根据下列需求完成数据库系统的设计。  
（1）记录创业服务中心和创业公司的信息。创业服务中心信息包括中心代码、名称、注册地址。创业公司信息包括公司代码、公司名称和联系电话。服务中心和创业公司的代码唯一且编码不同。  
（2）统一管理创业服务中心的员工。员工信息包括工号（唯一标识）、身份证号、姓名、性别、所属服务中心。  
（3）记录投资方的信息，包括投资方代码（唯一标识）、名称、电话。  
（4）投资方和创业公司之间依靠创业服务中心牵线建立创业项目合作关系，具体由创业服务中心的一位员工负责协调投资方和创业公司的创业项目。一个创业项目可属于多个创业公司，但可以接受多个投资方投资。项目包括属性项目编号、投资时间和金额，每个项目只有一个项目编号。请根据以上情况  
   1. 画出完整的E-R图。  
   2. 把E-R图转换为关系模式，并指出主码和外码  
   >> 答案,`PK`为主码

   1. 
   ```mermaid
   erDiagram
      SERVICE_CENTER {
         string center_id PK "中心代码"
         string name "名称"
         string address "注册地址"
      }

      STARTUP_COMPANY {
         string company_id PK "公司代码"
         string name "公司名称"
         string phone "联系电话"
      }

      EMPLOYEE {
         string emp_id PK "工号"
         string id_number "身份证号"
         string name "姓名"
         string gender "性别"
      }

      INVESTOR {
         string investor_id PK "投资方代码"
         string name "名称"
         string phone "电话"
      }

      PROJECT {
         string project_id PK "项目编号"
         date invest_date "投资时间"
         float amount "投资金额"
      }

      SERVICE_CENTER ||--o{ EMPLOYEE : "拥有"
      EMPLOYEE ||--o{ PROJECT : "协调"
      INVESTOR ||--o{ PROJECT : "投资"
      PROJECT ||--o{ STARTUP_COMPANY : "面向"
   ```
   2. 关系模式设计
      1. 创业服务中心
         - `中心代码` 主码
         - `名称`
         - `注册地址`
      2. 创业公司
         - `公司代码` 主码
         - `公司名称`
         - `联系电话`
      3. 员工
         - `工号` 主码
         - `身份证号`
         - `姓名`
         - `性别`
         - `所属服务中心` 外码，关联`ServiceCenter(center_id)`
      4. 投资方
         - `投资方代码` 主码
         - `名称`
         - `电话`
      5. 项目
         - `项目编号`
         - `投资时间`
         - `金额`
      6. 项目-投资方关联表
         - `项目编号` 主码，外码，关联`Project(project_id)`
         - `投资方代码` 主码，外码，关联`Investor(investor_id)`
      7. 项目-公司关联表
         - `项目编号` 主码，外码，关联`Project(project_id)`
         - `公司代码` 主码，外码，关联`StartupCompany(company_id)`

2. 某足球联赛拟开发一套信息管理系统，有以下要求：   
（1）系统需要维护球队、球员、主教练、主裁判和比赛等信息。
其中  
球队包括：球队编号、名称、成立时间、人数、主场地址、主教练；  
球员包括：姓名、身份证号、出生日期、身高、家庭地址；  
主教练包括：姓名、身份证号、教练证书号、教练级别；  
主裁判包括：姓名、身份证号、裁判证书号、获证时间、裁判级别。  
（2）每支球队有唯一的球队编号，也有一名主教练和若干球员。一名主教练只能受聘于一支球队，一名球员只能效力于一支球队。每支球队都有自己唯一的主场地并且不能共用。  
（3）比赛采用主客场循环制，一周进行一轮比赛，一轮所有比赛同时进行。  
（4）一场比赛有唯一比赛标号，只有两只球队参加，一为主队，另一为客队，只能有一个主裁判，需要记录比赛日期和成绩。  
根据以上需求，请完成以下任务。  
   1. 画出完整的E-R图。
   2. 把E-R图转换为关系模式，并指出主码和外码。
   >> 答案

   1. 
   ```mermaid
   erDiagram
      TEAM {
         int team_id PK "球队编号"
         string name "球队名称"
         date founded_date "成立时间"
         int num_players "人数"
         string home_address "主场地址"
      }

      PLAYER {
         string id_number PK "身份证号"
         string name "姓名"
         date birth_date "出生日期"
         float height "身高"
         string home_address "家庭地址"
      }

      COACH {
         string id_number PK "身份证号"
         string name "姓名"
         string license_number "教练证书号"
         string level "教练级别"
      }

      REFEREE {
         string id_number PK "身份证号"
         string name "姓名"
         string license_number "裁判证书号"
         date certified_date "获证时间"
         string level "裁判级别"
      }

      MATCH {
         int match_id PK "比赛编号"
         date match_date "比赛日期"
         string score "成绩"
      }

      TEAM ||--o{ PLAYER : "拥有球员"
      TEAM ||--|| COACH : "聘用教练"
      MATCH }o--|| TEAM : "主队"
      MATCH }o--|| TEAM : "客队"
      MATCH }o--|| REFEREE : "主裁判"
   ```
   2. 关系模式设计
      1. 球队
         - `球队编号` 主码
         - `名称`
         - `成立时间`
         - `人数`
         - `主场地址`
         - `主教练` 外码，关联`COACH(id_number)`
      1. 球员
         - `姓名`
         - `身份证号` 主码
         - `出生日期`
         - `身高`
         - `家庭地址`
         - `所属球队` 外码，关联`TEAM(team_id)`
      2. 主教练
         - `姓名`
         - `身份证号` 主码
         - `教练证书号`
         - `教练级别`
      3. 主裁判
         - `姓名`
         - `身份证号` 主码
         - `裁判证书号`
         - `获证时间`
         - `裁判级别`
      4. 比赛
         - `比赛标号` 主码
         - `比赛时间`
         - `得分情况`
         - `主场队伍` 外码，关联`TEAM(team_id)`
         - `客场队伍` 外码，关联`TEAM(team_id)`
         - `主裁判` 外码，关联`REFEREE(id_number)`

1. 某学生信息管理系统的部分数据库关系如下：   
学生：`S(sno, sname, sage, gender, schno)`，各属性分别表示学生学号、姓名、年龄、性别和所在学院，学号为主键。  
学院：`Sch(schno, schname, schstunum)`，各属性分别表示学院编号、名称和学生数，学院编号为主键。  
俱乐部：`C(cno, cname, cyear, cloc)`，各属性分别表示俱乐部编号、名称、成立时间和活动地点，俱乐部编号为主键。  
参加：`J(sno, cno, jyear)`，各属性分别表示参加俱乐部学生的学号、参加的俱乐部编号和参加年份。  
试用SQL语句表示下列操作：  
   1. 创建学生表，要求学生学号为CHAR(15)，学院编号为CHAR(4)，学生的姓名不能为空，性别取值为“F”和“M”，分别表示女性和男性，schno为外键。  
   2. 查询成立时间在‘2001-1-1’和‘2003-12-30’之间的所有俱乐部的名称和活动地点。  
   3. 查询所有姓为方且名为三个字的学生完整信息。  
   4. 添加一条俱乐部信息入库，具体内容为俱乐部编号为J0005，名称为高尔夫俱乐部，成立时间为2024年6月1日，活动地点未定。
   5. 删除学号S0088的学生在2024年参加俱乐部的记录。
   6. 修改高尔夫俱乐部的活动地点为学校东区高尔夫球场。
   7. 统计每个俱乐部有多少学生，输出为俱乐部名,学生数。
   8. 查询高尔夫俱乐部最年轻的学生姓名，假设只有一位学生。
   >> 答案

   1. ```sql 
      CREATE TABLE S(
         sno CHAR(15) PRIMARY KEY,
         sname VARCHAR(50) NOT NULL,
         sage INT,
         gender CHAR(1) CHECK (gender IN ('F', 'M')),
         schno CHAR(8),
         FOREIGN KEY (schno) REFERENCE Sch(schno)
      );
      ```
   2. ```sql
      SELECT cname, cloc
         FROM C
         WHERE cyear BETWEEN '2001-1-1' AND '2003-12-30';
      ```
   3. ```sql
      SELECT *
         FROM S
         WHERE sname LIKE '方__';
      ```
   4. ```sql
      INSERT INFO C(cno, cname, cyear, cloc)
         VALUES('J0005', '高尔夫俱乐部', '2024-6-1', NULL);
      ```
   5. ```sql
      DELETE 
         FROM J
         WHERE sno = 'S0088' AND jyear = '2024'
      ```
   6. ```sql
      UPDATE C
         SET cloc = '学校东区高尔夫球场'
         WHERE cname = '高尔夫俱乐部'
      ```
   7. ```sql
      SELECT C.cname, COUNT(J.sno) as stu_cnt
         FROM C
         LEFT JOIN J ON C.cno = J.cno
         GROUP BY C.cname;
      ```
   8. ```sql
      SELECT S.sname
         FROM S
         JOIN J ON S.sno = J.sno
         JOIN C ON J.cno = C.cno
         WHERE C.cname = '高尔夫俱乐部'
         ORDER BY S.sage ASC
         LIMIT 1;
      ```

4. 根据上题系统设计触发器，每当系统中新增加一个学生，就需要自动修改相应学院的学生数，以保持系统中学生人数的完整性与一致性。
   ```sql
   DELIMITER
      CREATE TRIGGER updater
         AFTER INSERT ON S
         FOR EACH ROW
         BEGIN
            UPDATE Sch
            SET schStuNum = schStuNum + 1
            WHERE schno = NEW.schno;
         END
   DELIMITER;
   ```

5. 某汽车租赁公司建立汽车租赁管理系统，数据库的部分模式如下：   
租用用户：`U(uid, name, balance)`，属性为用户编号（主键）、姓名和余额；  
待租汽车：`C(cid, type, price, status)`，属性为汽车编号（主键）、类型、日租价格和状态；  
租用记录：`B(bid, uid, cid, btime, etime)`，属性表示租用编号（主键）、用户编号、汽车编号、起租时间、归还时间，bimte和etime为date数据类型；  
不良记录：`S(sid, uid, bid, stime)`，属性为不良记录编号、用户编号、记录编号和不良记录时间。  
试用SQL语句表示下列操作：  
   1. 创建租用记录表，要求各编号的数据类型都为char(8)，起租时间不能为空，定义合适的主键和外键。
   2. 查询所有姓为唐且名不少于一个字的租用用户的用户的编号和余额。
   3. 查询租用记录中不少于25天租期的所有记录的完整信息。
   4. 添加一条待租汽车入库，具体内容为汽车的编号为C0010，类型为Sports Car，日租价格为800，状态为就绪。
   5. 在租用记录中删除起租时间为2025年的所有记录。
   6. 在待租汽车中修改所有类型为Sports Car的汽车状态为维护中。
   7. 统计每种类型汽车的租用次数，输出为汽车类型, 租用次数。
   8. 查询Sports Car汽车最长租期的租用用户的所有信息，假设最长租期只有一位用户。
   >> 答案

   1. ```sql
      CREATE TABLE B(
         bid CHAR(8) PRIMARY KEY,
         uid CHAR(8),
         cid CHAR(8),
         btime DATE NOT NULL,
         etime DATE,
         FOREIGN KEY (uid) REFERENCES U(uid),
         FOREIGN KEY (cid) REFERENCES C(cid)
      );
      ```
   2. ```sql
      SELECT uid, balance
         FROM U
         WHERE name LIKE '唐%';
      ```
   3. ```sql
      SELECT *
         FROM B
         WHERE DATEDIFF(etime - btime) >= 25
      ```
   4. ```sql
      INSERT INFO C(cid, type, price, status)
         VALUES('C0010', 'Sports Car', 800, '就绪');
      ```
   5. ```sql
      DELETE
         FROM B
         WHERE YEAR(btime) = 2025;
      ```
   6. ```sql
      UPDATE C
         SET status = '维护中'
         WHERE type = 'Sports Car';
      ```
   7. ```sql
      SELECT C.type, COUNT(B.bid) as cnt
         FROM C
         LEFT JOIN B ON C.cid = B.cid
         WHERE C.type = 'Sports Car';
      ```
   8. ```sql
      SELECT U.*
         FROM B
         JOIN C ON B.cid = C.cid
         JOIN U ON B.uid = U.uid
         WHERE C.type = 'Sports Car'
         ORDER BY DATEDIFF(etime - btime) DESC
         LIMIT 1
      ```

6. 根据上题系统设计触发器，当归还时间为空时表示用户尚未还车，系统每天调用事务程序从用户余额中自动扣除当日租金，每个事务修改一条用户记录中的余额。由用户表上的触发器实现业务：如果用户单日余额不足，不扣除当日租金，但自动向不良记录加入一条记录，记录中的`bid`取值由uid +系统当前日期构成，可以采用`CURRENT_DATE`函数获取当前时间。
   ```sql
   DELIMITER
      CREATE TRIGGER trigger
         AFTER UPDATE U
         FOR EACH ROW
         BEGIN
            IF NEW.balance = OLD.balance THEN
               INSERT INFO S(sid, uid, bid, stime)
                  SELECT CONCAT(NEW.uid, DATE_FORMAT(CURRENT_DATE, '%Y%m%d')), NEW.uid, CONCAT(NEW.uid, DATE_FORMAT(CURRENT_DATE, '%Y%m%d')), CURRENT_DATE
                  FROM B
                  WHERE B.uid = NEW.uid AND B.etime IS NULL
                  LIMIT 1
            END IF;
         END;
   DELIMITER;
   ```