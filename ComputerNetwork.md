# Computer Network
## 概述
1. 计算机网络的目的： 进行*信息交换*和*资源共享*（P5）
2. 计算机网络的分类： 
    1. 按作用范围： 广域网(WAN)，城域网(MAN), 局域网(LAN)和个人区域网(PAN)；
    2. 按使用者： 共用网和专用网
3. 计算机网络的性能指标： 速率，带宽，吞吐量，时延（**发送时延和传播时延的计算，三种常用介质**），时延带宽积，往返时间RTT，利用率（P21）
    1. 发送时延：$发送时延 = \frac{数据帧长度(bit)}{发送速率(bit/s)}$   
    2. 传播时延： $传播时延 = \frac{信道长度(m)}{电磁波中信道上的传播速率(m/s)}$
        > 信道的发送速率$2 * 10^{3}bit/s$, 光纤传输$200m$，传输时延为$\frac{200m}{2 * 10^8} = 10^{-6}s$
    3. 常见介质：电磁波在自由空间的传播速率是$3 * 10 ^ {5} km/s$, 在铜线电缆上的传播速率为$2.3 * 10^{5} km/s$，在光纤中的传播速率为$2 * 10^{5} km/s$
4. 网络协议的要素：
    1. *语法*，即数据与控制信息的结构或格式
    2. *语义*，即需要发出何种控制信息，完成何种动作以及做出何种相应
    3. *同步*，即事件实现顺序的详细说明
5. OSI七层协议体系结构：
    1. 物理层
    2. 数据链路层
    3. 网络层
    4. 运输层
    5. 会话层
    6. 表示层
    7. 应用层

## 物理层
1. 物理层的基本概念：
    1. *机械特性*，指明接口所用接线器的形状和尺寸、引脚数目和排列、固定和锁定装置等。
    2. *电气特性*，指明在接口电缆的各条线上出现的电压范围
    3. *功能特性*，指明某条线上出现的某一电平的电压的意义
    4. *过程特性*，指明对于不同功能的各种可能事件的出现顺序
2. 信噪比(见计算题)

## 数据链路层
1. 数据链路层的功能：（P73）
    1. *封装成帧*
    2. *透明传输*
    3. *差错检测*
2. 字节填充：
    1. $0x7D -> 0x7D 5D$
    2. $0x7E -> 0x7D 5E$
    3. $0xab(< 0x20) -> 0x7D 23$
    > 一个PPP帧的数据部分是$0x 7D 5E FE 27 7D 5D 7D 5D 65 7D 5E$。则真正的数据是什么？  
    $0x 7E FE 27 7D 7D 65 7E$
3. 零比特填充：
    1. 只要发现有5个连续1, 则立即填入一个0
4. MAC层
5. MAC帧
6. 扩展以太网
    1. 冲突域

## 网络层
1. 网络协议IP：
    1. 地址解析协议ARP
    2. 网际控制报文协议ICMP
    3. 网际诅管理协议IGMP
2. 转发表（见计算题）
3. 子网划分（见计算题）

## 运输层
1. 主要协议： TCP（面向连接），UDP
2. 常用端口号： FTP(port 21), TELNET(port 23), SMTP(port 25), DNS(port 53), TFTP(port 69), HTTP(port 80), SNMP(port 161), SNMP(trap, port 162), HTTPS(port 443)
3. TCP三次握手
    1. C端进程
4. TCP的拥塞控制方法
    1. 慢开始： 由小到大逐渐增大注入到网络中的数据字节（线性增加，k = 1）
    2. 拥塞避免：发生拥塞后，cwnd = 慢开始门限 / 2

## 应用层
1. DNS
2. FTP
3. WWW
4. HTTP
5. SNMP

## 计算题
1. > 循环冗余校验(CRC)

    $M = 110 110, G(x) = x^{4} + x^{2} + 1$计算校验位
    > $110 110 0000 / 10101 = 111 011 ... 0111$, 得出校验为$11 0110 0111$
2. > 奈氏准则（无噪声信道） 
    $Baud$ 波特率（$码原/s$） = 2w
    $比特率 = 2w * 2$
   <!-- P46 -->
    $w = 2kHz, 8电平$， 则比特率为$2 * 2k * 3 = 12000bps$  
    > 香农公式（有噪声信道）
    $C = Wlog(1 + S / N)$，其中，$S/N$为信噪比，$1dB = 10log_{10}(S/N)$

    $w = 2kHz, 信噪比为20dB, 求C$
    $S/N = 10^{\frac{20dB}{10}}, C = 2000log(1 + 1000) = 20k$
3. > 运输层的拥塞控制  

    $cwnd = 1, 慢开始门限 = 8,在第5轮时发生拥塞，求RTT1到RTT8轮的cwnd的变化$
    | RTT | cwnd |
    | --- | ---  |
    | RTT1 | 1 |
    | RTT2 | 2 |
    | RTT3 | 4 | 
    | RTT4 | 8 |
    | RTT5 | 4(发生拥塞， 门限 = 拥塞窗口值 / 2) |
    | RTT6 | 5 |
    | RTT7 | 6 |
    | RTT8 | 7 |  

    若慢开始门限 = 16, 在第6轮发生拥塞，则RTT1 - RTT8的cwnd的变化为？
    | RTT | cwnd |
    | --- | ---  |
    | RTT1 | 1 |
    | RTT2 | 2 |
    | RTT3 | 4 | 
    | RTT4 | 8 |
    | RTT5 | 16 |
    | RTT6 | 8 |
    | RTT7 | 9 |
    | RTT8 | 10 | 

    若初始cwnd = 2?
    | RTT | cwnd |
    | --- | ---  |
    | RTT1 | 2 |
    | RTT2 | 4 |
    | RTT3 | 8 | 
    | RTT4 | 16 |
    | RTT5 | 17 |
    | RTT6 | 8 |
    | RTT7 | 9 |
    | RTT8 | 10 |

## 应用题
1. 转发表
    已知路由器$R_{1}$的转发表如表所示
    | 前缀匹配 | 下一跳地址 | 路由器接口 | 
    | --- | --- | --- |
    | 140.5.12.64/26 | 180.15.2.5 | m2 |
    | 130.5.8/24 | 190.16.6.2 | m1 |
    | 110.71/16 | --- | m0 |
    | 180.15/16 | --- | m2 |
    | 190.16.16 | --- | m1 |
    | 默认 | 110.71.4.5 | m0 |
    给定ip地址得出走的接口

